services:
  api:
    image: python:3.11-slim
    working_dir: /app
    command: sh -c "pip install -r requirements.txt && python main.py"
    ports:
      - "3233:3233"
    environment:
      DOCKERMAP_HOST: 0.0.0.0
      DOCKERMAP_PORT: 3233
    volumes:
      - .:/app
